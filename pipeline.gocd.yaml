pipelines:
  test_and_deploy:
    group: email-classification
    materials:
      email-classification-git:
        git: https://github.com/davified/email-classification.git
    stages:
      - test:
          jobs:
            resources:
              - go-agent-1
            unit_tests:
              tasks:
               - exec:
                   command: bash
                   arguments: echo "hello gocd"
